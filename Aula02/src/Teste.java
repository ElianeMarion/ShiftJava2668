public class Teste {
    public static void main(String[] args) {
        /*Estrutura de decisão
            comando de decisão e uma condição
            condição -> variável opRel valor ou variável
                            idade >= 18
           Decisão simples
           if (condicao){
                //comando
           }
           comandos
           Decisão composta
           if(condição)
              //comando V
           else
               //comando F

           Decisão encadeada
           if(condição){
               //comando v
           }
           else if (condição2)
               comando v2
           else
               comando F

           switch()
        */
    }
}
